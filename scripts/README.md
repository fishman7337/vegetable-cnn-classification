# Scripts

Utility scripts for repository maintenance live here.

- `split_notebook.py`: regenerates source-preserving notebook sections from `notebooks/DELE_CA1_A.ipynb` and verifies notebook integrity in CI.

Prefer adding small, documented scripts here when a task needs to be repeated locally or in CI.
