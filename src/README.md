# Source

Python package source code lives under this folder.

- `vegetable_vision/`: reusable project package for configuration, data helpers, model builders, training, evaluation, and notebook tooling.

The project uses a `src/` layout so imports behave consistently between local development and CI.
