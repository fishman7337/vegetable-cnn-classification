# Data

This folder is reserved for local datasets and generated dataset variants.

- `raw/`: original source data, kept out of git.
- `processed/`: cleaned or transformed data, kept out of git.

Commit only documentation and placeholder files here. Do not commit image datasets or private data.
