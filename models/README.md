# Models

Model checkpoints, `.keras`, `.h5`, `.weights.h5`, ONNX, and TFLite artifacts are generated here and ignored by git.

Keep model lineage in `reports/evaluation.json`, model cards, experiment notes, or an external artifact store.
