# Tests

Pytest tests for reusable project logic live here.

The current suite covers:

- environment and training configuration helpers
- dataset layout and cleanup helpers
- notebook splitting utilities

Full model training is intentionally not part of the default test suite because it requires the original dataset and GPU-capable compute.
