# Workflows

GitHub Actions workflows live here.

- `ci.yml`: runs formatting, linting, tests, notebook integrity checks, Bandit, and pip-audit.
- `codeql.yml`: runs CodeQL static analysis for Python.

Keep workflow changes aligned with the local commands documented in the root README.
